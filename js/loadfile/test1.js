// for(var i = 0; i<)
function delay(time) {
	var  s = '';
	for (var i = 0; i < time; i++) {
		// s += Math.random();
		var scrpitEl = document.createElement('img');
		document.head.appendChild(scrpitEl)
		// img.src = 'http://www.google.com.hk/images/srpr/logo11w.png?random=' + (Math.random());
		// document.body.appendChild(img);
	}
}
// delay(10);
// delay(10);
delay(100);